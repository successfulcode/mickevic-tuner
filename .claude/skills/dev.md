# /dev

Start the Vite development server for mickevic-tuner.

## Steps

1. Run `npm install` if `node_modules` does not exist
2. Run `npm run dev` in the background
3. Report the local URL printed by Vite (typically http://localhost:5173)
4. Mention that the server is running and hot-reload is active
