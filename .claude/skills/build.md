# /build

Build the mickevic-tuner project for production.

## Steps

1. Run `npm run build`
2. Report the output directory (`dist/`) and bundle sizes from Vite's output
3. If the build fails, show the error and suggest a fix
